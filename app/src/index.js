import React from 'react';
import ReactDOM from 'react-dom';
// React DOM est le module qui va nous permettre de crféer le virtual DOM

import App from './App';
import './index.css';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
